import React from 'react';

const ArticleList = ({ post }) => {
    return (
        <div>
            {/* <main>
                {post.map((post) => {
                    return (
                        <Article
                            key={post.id}
                            date={post.date}
                            preview={post.preview}
                        ></Article>
                    );
                })}
            </main> */}
        </div>
    );
};

export default ArticleList;
